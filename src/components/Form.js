import styled from 'styled-components'

var Form = styled.form`
  button {
    margin-left: 10px;
    padding: 0px 10px;
  }

  input {
    margin-bottom: 10px;
    padding: 10px 0px;
  }
`

export default Form
